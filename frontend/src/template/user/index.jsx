import React from 'react';
import Styled from './styled';

const UserTemplate = ({ userId }) => {
  return <Styled.MainContainer>{userId} haha</Styled.MainContainer>;
};

export default UserTemplate;
