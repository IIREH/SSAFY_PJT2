import styled from '@emotion/styled';

const MainContainer = styled.div`
  text-align: center;
  justify-content: start;
  position: relative;
  color: green;
`;

export default {
  MainContainer,
};
